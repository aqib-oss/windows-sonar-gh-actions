name: "conda_windows_sonar_gh_actions"
channels:
  - conda-forge
  - defaults
  - anaconda
dependencies:
  - python=3.11
  - ipykernel
  - pip
  - pip:
      - poetry
      - twine
      - python-semantic-release
      - pre-commit
      - black
      - flake8
      - black[jupyter]
